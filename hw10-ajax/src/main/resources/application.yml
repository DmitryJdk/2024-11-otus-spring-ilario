spring:
  application:
    name: hw10-ajax
  mvc:
    hiddenmethod:
      filter:
        enabled: true
mongock:
  change-logs-scan-package:
    - ru.otus.hw.mongock
  runner-type: InitializingBean
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG